pub fn log_info(message: &str) {
    log::info!("{}", message);
}

pub fn log_error(message: &str) {
    log::error!("{}", message);
}

pub fn log_warn(message: &str) {
    log::warn!("{}", message);
}
